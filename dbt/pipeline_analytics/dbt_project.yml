name: 'pipeline_analytics'
version: '1.0.0'

profile: 'pipeline_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  pipeline_analytics:
    staging:
      +materialized: table
    marts:
      +materialized: table

seeds: 
  pipeline_analytics:
    dim_leitos:
      +delimiter: ";"
